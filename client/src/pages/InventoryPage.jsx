import React, { useEffect, useState } from 'react';
import Inventory from '../components/Inventory';

const InventoryPage = (character_id) => {
  <Inventory characterId={character_id}/>
};

export default InventoryPage;
